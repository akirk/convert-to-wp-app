{
    "name": "akirk/convert-to-wp-app",
    "description": "Convert an existing frontend app into a WordPress plugin powered by WpApp",
    "type": "project",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4",
        "akirk/create-wp-app": "^1.0"
    },
    "archive": {
        "exclude": [
            "/.git"
        ]
    },
    "scripts": {
        "post-create-project-cmd": "php scripts/convert.php"
    }
}
